# KRISHNA-3
GAME CHANGER

## Atlas payouts demo

The `atlas/` directory contains a separate Next.js application that demonstrates a **double-entry ledger**, **idempotent transfers**, and **non-float money handling** for a payouts-style wallet. See `atlas/README.md` for how to run it locally and for explicit limitations of this slice.

